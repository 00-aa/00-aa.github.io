  /*
  import { OnChildAdded } from '/firebase.js';
  
  function Init(){
    OnChildAdded('APP/BANNERS',(BannerImgData)=>{
      let Banner = BannerImgData.val();
      banner.innerHTML += `
        <img loading="lazy" src="${Banner.ImageUrl}" id="banner_img" name="${BannerImgData.key}">
      `;
    });
    
  }
  
  addEventListener('load',Init)
  */